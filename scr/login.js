// Sau khi nhận được phản hồi từ máy chủ
const validAlert = document.getElementById("validAlert");

if (data === "no" || data == "Username cannot found") {
    loginForm.classList.remove("hidden");
}
